server:
  port: 8081

spring:
  application:
    name: NextFirstTagManagement

  datasource:
    url: jdbc:mysql://localhost:3306/tagmanagement
    username: root
    password: mysql
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQL8Dialect
